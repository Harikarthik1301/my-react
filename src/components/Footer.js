import React from "react"

const Footer = ({items}) => {
    
  return (
    {items}?
    <div className='footer'><p>Total Animes : {items.length}</p>
    </div>
    :<h1>not working</h1>

  )
}

export default Footer